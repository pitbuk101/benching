SELECT
    YEAR,
    CATEGORY,
    COUNT(DISTINCT SUPPLIER) AS SUPPLIER_WITH_OPPORTUNITY
FROM
    DATA.T_C_WEB_PRICE_BENCHMARK
WHERE
    YEAR = YEAR(CURRENT_DATE)
    AND LOWER(CATEGORY) = LOWER('Valves')
    AND SAVING_OPPORTUNITY > 0
GROUP BY 
    YEAR,
    CATEGORY
